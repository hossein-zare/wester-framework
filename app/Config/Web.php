<?php

    $config = [];

    // Path to the framework
    $config['path'] = '';

    // Default locale
    $config['locale'] = 'en';

    // Fallback locale
    $config['fallback_locale'] = 'en';
    
    // Application Key
    $config['key'] = '(PXLzl3bEeIs2SRH'; // Required...!
    
    // Debug mode
    $config['debug'] = true;
