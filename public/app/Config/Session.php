<?php

    $config_session = [];

    /**
     * Default Lifetime
     */
    $config_session['lifetime'] = 1440;

    /**
     * Http-Only (Common with Cookies)
     */
    $config_session['http_only'] = false;

    /**
     * Secure (Common with Cookies)
     */
    $config_session['secure'] = false;
